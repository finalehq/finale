{
  "name": "finale",
  "private": true,
  "workspaces": [
    "packages/*",
    "platforms/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "npm run build",
    "dev": "npm run dev",
    "lint": "npm run lint"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
