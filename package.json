{
  "name": "finale",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared/*"
  ],
  "scripts": {
    "app:dev": "npm run dev --workspace=@finale/app",
    "app:build": "npm run build --workspace=@finale/app",
    "app:preview": "npm run preview --workspace=@finale/app",
    "app:lint": "npm run lint --workspace=@finale/app",
    "docs:dev": "npm run dev --workspace=@finale/documentation",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
